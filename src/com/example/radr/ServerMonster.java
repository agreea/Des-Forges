package com.example.radr;

public class ServerMonster {
// RefreshFeed()
	// Ask the server for the N posts within K miles of your location
	// Store the posts or check against what you have
	// Send the posts
	
// addPost()
	// Send the post to the server (Async)
	// Fetch the (url)post from the server 
	// JOSH CAN WORRY ABOUT THIS: Store relevant information....
	// Add the post to the complexPrefs
	// Add the post's KEY, TIMESTAMP, LAT, LONG to the DB
	// Add the post to the feed...? Or return URLPost and let frontEnd add it to feed.
	
// DeletePost(POSTID)
	// Tell the server to delete the post (Async)
	// Change information in storage (idk....)

}
